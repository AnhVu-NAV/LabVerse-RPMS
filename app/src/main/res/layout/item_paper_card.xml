<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="12dp"
    app:cardUseCompatPadding="true"
    app:cardElevation="2dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <ImageView
            android:id="@+id/paper_thumbnail"
            android:layout_width="0dp"
            android:layout_height="100dp"
            android:scaleType="centerCrop"
            android:background="@drawable/thumbnail_bg"
            android:padding="8dp"
            android:contentDescription="@string/cd_paper_thumbnail"
            app:layout_constraintDimensionRatio="1:1"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toEndOf="@id/guideline"
            tools:src="@tools:sample/backgrounds/scenic" />

        <androidx.constraintlayout.widget.Guideline
            android:id="@+id/guideline"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            app:layout_constraintGuide_percent="0.66" />

        <TextView
            android:id="@+id/paper_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:textStyle="bold"
            android:textSize="16sp"
            android:textColor="@color/text_primary"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toStartOf="@id/paper_thumbnail"
            android:layout_marginEnd="16dp"
            tools:text="The Impact of AI on Education" />

        <TextView
            android:id="@+id/paper_authors"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textSize="14sp"
            android:textColor="@color/text_secondary"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/paper_title"
            app:layout_constraintEnd_toStartOf="@id/paper_thumbnail"
            android:layout_marginEnd="16dp"
            tools:text="Dr. Emily Carter, Dr. David Lee" />

        <com.google.android.material.chip.Chip
            android:id="@+id/paper_status_chip"
            style="@style/Widget.MaterialComponents.Chip.Choice"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:textSize="14sp"
            android:textStyle="normal"
            android:textColor="#0D141C"
            app:chipIconVisible="true"
            app:chipIconSize="18dp"
            app:iconStartPadding="4dp"
            app:chipMinHeight="32dp"
            app:chipStartPadding="16dp"
            app:chipEndPadding="8dp"
            app:chipIconTint="#0D141C"
            app:ensureMinTouchTargetSize="false"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/paper_authors"
            tools:text="Reading"
            tools:chipIcon="@drawable/ic_chip_reading" />

        <ProgressBar
            android:id="@+id/paper_progress_bar"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="0dp"
            android:layout_height="6dp"
            android:layout_marginTop="12dp"
            android:visibility="gone"
            android:progress="60"
            android:progressTint="@color/primary"
            android:progressBackgroundTint="@color/progress_track"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/paper_status_chip"
            app:layout_constraintEnd_toStartOf="@id/paper_thumbnail"
            android:layout_marginEnd="16dp"
            tools:visibility="visible"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</com.google.android.material.card.MaterialCardView>
